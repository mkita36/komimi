<div class="row border border-secondary border-top-0 align-items-center py-2">
  <div class="col-1">
    <i class="fas fa-reply"></i>
  </div>
  <%= render 'profiles/image', profile: Profile.find_by(user: reply.user) %>
  <div class="col-6">
    <div class="row">
      <%= User.find(reply.user_id).name %>
    </div>
    <div>
      <%= simple_format(reply.comment) %>
    </div>
  </div>
</div>